{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "main.go",
            "env": {
                "YUDAI_DB_URL": "postgres://postgres:password@localhost/yudai?sslmode=disable"
                // "YUDAI_DB_URL": "host=localhost user=postgres password=password dbname=yudai sslmode=disable"
            }
        }
    ]
}